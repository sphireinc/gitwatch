# Task 30 — Cross-platform CI and quality gates

**Priority:** P0

GitHub Actions matrix for supported Go/OS targets. Run tests, vet, lint, race where available, vulnerability/static analysis chosen by project, and build. Cache Go modules safely. Add dependency update automation if desired.

**Acceptance:** Protected main can require all P0 CI checks; Windows path behavior is genuinely tested, not just cross-compiled.
