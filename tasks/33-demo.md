# Task 33 — Demo assets and visual polish pass

**Priority:** P1

Create a deterministic demo repository/script that produces attractive mixed status states for screenshots and terminal recordings. Polish spacing, truncation, border hierarchy, status glyphs, animations, clean-state screen, and error modals across common terminal emulators. Do not rely on Nerd Fonts by default; optionally enhance when detected/configured.

**Acceptance:** README media demonstrates live refresh, mouse selection, stage/unstage, filter, diff, responsive resizing.

**Status:** In progress — the deterministic mixed-state fixture, capture guide,
static README image, and `docs/demo.cast` provide genuine evidence for startup,
keyboard diff, reversible keyboard stage/unstage, a wide-to-narrow redraw, and
clean quit. Existing media does not demonstrate external live refresh, mouse
selection, fuzzy filtering, or the narrow diff/details overlay. Task 33 remains
in progress while replacement media is prepared; it must not be treated as
complete until current-candidate README-linked media shows every
acceptance item listed above and records the capture environment described in
`docs/demo.md`.
